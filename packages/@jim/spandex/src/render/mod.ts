/**
 * @module
 *
 * Rendering backend interface for spatial index visualization.
 *
 * Provides abstract renderer interface and types. Implementations in `@jim/spandex-ascii` and `@jim/spandex-html`.
 */

export * from './renderer.ts';
export * from './types.ts';
